# Theryn server preset — copy sections into config.yml or set preset: theryn (if loader supports merge)
# This file is reference-only unless copied into the live config.

integrations:
  lands:
    enabled: true
    check-at: BOTH
    wilderness: DENY
    wilderness-permission: itemmagnet.wilderness
    claimed-land: MEMBER_ONLY
    require-player-in-allowed-land: true
  worldguard:
    enabled: true
    check-at: BOTH
    respect-item-pickup-flag: true
    region-mode: BLACKLIST
    regions:
      - archive_landing
      - spawn

anti-afk:
  enabled: true
  required-blocks-moved: 2
  window-seconds: 60
  disable-auto-fuel-when-afk: true

height:
  use-y-range: false
  underground:
    enabled: true
    threshold-y: 50
    radius-modifier: -2
    drain-multiplier: 1.25

tiers:
  fragment:
    display-name: "&6Fragment Resonator"
    unlock:
      type: CMI_STAT
      stat: blocksmined
      sub: DEEPSLATE
      amount: 5000
  survey:
    unlock:
      type: ADVANCEMENT
      advancement: itemmagnet:unlock_survey
  anchor:
    unlock:
      type: COMMAND

# CMI event command example:
# - "itemmagnet unlock [playerName] fragment"

# Optional proximity-lore example (copy into live config; keep enabled: false in preset merge):
# proximity-lore:
#   enabled: true
#   zones:
#     wilderness_shrine:
#       world: Theryn
#       x: 1200
#       y: 64
#       z: -800
#       radius: 12
#       messages:
#         - "&8The resonator hums: &7The silence here is older than spawn."
